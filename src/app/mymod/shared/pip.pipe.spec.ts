/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PipPipe } from './pip.pipe';

describe('Pipe: Pip', () => {
  it('create an instance', () => {
    let pipe = new PipPipe();
    expect(pipe).toBeTruthy();
  });
});
